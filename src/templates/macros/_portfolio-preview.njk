{% macro settings(name, url, src) %}

<div class="gallery__item">
   <img src="{{ src }}" alt="{{ name }} thumbnail image">
   <a href="/portfolio/{{ url }}" class="gallery__link link_style-primary">
      {{ name }}
   </a>
</div>

{% endmacro %}